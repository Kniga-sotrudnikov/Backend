from django.db import models
exit
from structure.models import Department
# Посмотрим, есть ли вообще записи
print(f"Всего записей: {Department.objects.count()}")
# Посмотрим ID первой записи
first = Department.objects.first()
if first:
    print(f"ID первой записи: {first.id}")
else:
    print("В базе пусто!")
exit
